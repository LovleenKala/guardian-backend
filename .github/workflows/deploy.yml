name: Run Node.js Application

on:
  push:
    branches:
      - main 

jobs:
  guardian-app:
    runs-on: ubuntu-latest

    steps:
   
      - name: Check out the repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

     
      - name: Install dependencies
        run: npm install

   
      - name: Start the application
        env:
           MONGODB_URL: "mongodb://mongo:TTIDqLOFFZHpnBqXQOSkYTZCjHnlkytZ@autorack.proxy.rlwy.net:28357"

        run: |
          echo "Connecting to MongoDB at $MONGODB_URL" # Optional: Log for debugging
          node server.js # Replace 'index.js' with the entry point of your app
